spring:
  application:
    name: chargeback-ui
---
spring:
  cloud:
    config:
      env: default
---
spring:
  cloud:
    config:
      label: master

---

spring:
  cloud:
    config:
      uri: ${vcap.services.config-cb.credentials.uri:http://localhost:8081}      
      